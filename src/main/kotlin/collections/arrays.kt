package collections

// arrays are mutable and of fixed size.


fun main() {
    // syntax to define an array in kotlin
    val myArray = Array<Int>(5) { 0 }

    // to find the length of the array
    println(myArray.size)


}