package classes

// private will be visible inside same file
// public will be visible everywhere (default scope)
// internal will be visible inside same module
// protected will be visible inside same package, can't declare a high level function as protected