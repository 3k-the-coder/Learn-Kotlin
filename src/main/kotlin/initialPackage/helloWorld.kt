package initialPackage

fun main() {
    println("Hello World")
}

// kotlin creates a class internally, which is loaded into the memory for execution in the runtime

// but one thing to note down here is that we don't use static or any equivalent for that.
// because in kotlin there is no concept of static